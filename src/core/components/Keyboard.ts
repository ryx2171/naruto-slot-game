export function addKeyboardEvent(){

    document.addEventListener('keydown', (key)=>{

        //console.log("key",key.keyCode)
        //key Enter
        if(key.keyCode == 80){

        }

    });

}